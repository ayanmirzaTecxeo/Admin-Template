import { ICONS } from 'src/assets/library';

const navConfig = [
  {
    title: 'dashboard',
    path: '/',
    icon: ICONS.dashboard,
  },
  {
    title: 'user',
    path: '/user',
    icon: ICONS.user,
  },
];

export default navConfig;
